import './styles/AppStatus.css';

function AppStatus() {
  return (
    <div className="app-status">
      {/* Existing content of AppStatus */}

      {/* Overrides tab removed from AppStatus and moved to its own tab */}
    </div>
  );
}

export default AppStatus;